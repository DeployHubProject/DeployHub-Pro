apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dh-ms-nginx.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "dh-ms-nginx.name" . }}
    helm.sh/chart: {{ include "dh-ms-nginx.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "dh-ms-nginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
